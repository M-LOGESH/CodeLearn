---
import SidebarLayout from '../../../layouts/SidebarLayout.astro';
import "../../../styles/global.css";
---

<SidebarLayout app="css">
  <h1 class="text-xl sm:text-3xl font-bold mb-5 text-custom-accent text-center">
    CSS Grid
  </h1>

  <section class="mb-5 space-y-6 text-base sm:text-lg leading-relaxed text-custom-secondary">
    <p>
      CSS Grid is a two-dimensional layout system that allows you to create complex web layouts with rows and columns. It's perfect for overall page layout and component design.
    </p>

    <h2 class="text-xl sm:text-2xl font-semibold text-custom-accent mt-6 mb-3">Grid Container Properties</h2>

    <div class="bg-custom-secondary border border-custom rounded-lg p-3 sm:p-4 my-4">
      <pre class="text-xs sm:text-sm overflow-x-auto whitespace-pre-wrap"><code>{`.grid-container {
  display: grid;
  
  /* Define columns and rows */
  grid-template-columns: 1fr 2fr 1fr; /* Fractional units */
  grid-template-rows: 100px auto 100px;
  
  /* Gap between items */
  gap: 20px; /* row-gap and column-gap shorthand */
  
  /* Template areas for visual layout */
  grid-template-areas: 
    "header header header"
    "sidebar main aside"
    "footer footer footer";
  
  /* Auto placement */
  grid-auto-flow: row; /* row | column | dense */
}`}</code></pre>
    </div>

    <h2 class="text-xl sm:text-2xl font-semibold text-custom-accent mt-6 mb-3">Grid Item Properties</h2>

    <div class="bg-custom-secondary border border-custom rounded-lg p-3 sm:p-4 my-4">
      <pre class="text-xs sm:text-sm overflow-x-auto whitespace-pre-wrap"><code>{`.grid-item {
  /* Placement using line numbers */
  grid-column: 1 / 3; /* Start at line 1, end at line 3 */
  grid-row: 1 / 2;
  
  /* Placement using area names */
  grid-area: header;
  
  /* Placement using span */
  grid-column: 1 / span 2; /* Start at line 1, span 2 columns */
  
  /* Self alignment */
  justify-self: stretch; /* start | end | center | stretch */
  align-self: center; /* start | end | center | stretch */
}`}</code></pre>
    </div>

    <div class="bg-custom-secondary border border-custom rounded-2xl p-4 sm:p-6 mt-6">
      <h2 class="text-xl sm:text-2xl font-semibold text-custom-accent mb-3">Grid Units & Functions</h2>
      <ul class="list-disc list-inside space-y-2 pl-4">
        <li><strong class="text-custom-primary">fr unit</strong>: Fractional unit for flexible sizing</li>
        <li><strong class="text-custom-primary">repeat()</strong>: <code class="bg-custom-primary text-custom-secondary px-1 rounded text-sm">grid-template-columns: repeat(3, 1fr)</code></li>
        <li><strong class="text-custom-primary">minmax()</strong>: <code class="bg-custom-primary text-custom-secondary px-1 rounded text-sm">grid-template-columns: minmax(200px, 1fr)</code></li>
        <li><strong class="text-custom-primary">auto-fit/fill</strong>: Responsive columns that adapt to container size</li>
      </ul>
    </div>
  </section>
</SidebarLayout>