---
// File: src/pages/css/transitions-animations.astro
import SidebarLayout from '../../../layouts/SidebarLayout.astro';
import "../../../styles/global.css";
---

<SidebarLayout app="css">
  <h1 class="text-xl sm:text-3xl font-bold mb-5 text-custom-accent text-center">
    Transitions & Animations
  </h1>

  <section class="mb-5 space-y-6 text-base sm:text-lg leading-relaxed text-custom-secondary">
    <p>
      CSS transitions and animations bring interfaces to life by adding motion and interactivity. They enhance user experience and provide visual feedback.
    </p>

    <h2 class="text-xl sm:text-2xl font-semibold text-custom-accent mt-6 mb-3">CSS Transitions</h2>

    <div class="bg-custom-secondary border border-custom rounded-lg p-3 sm:p-4 my-4">
      <pre class="text-xs sm:text-sm overflow-x-auto whitespace-pre-wrap"><code>{`.button {
  background-color: #007bff;
  transition: all 0.3s ease;
  
  /* Individual properties */
  transition-property: background-color, transform;
  transition-duration: 0.3s;
  transition-timing-function: ease;
  transition-delay: 0s;
}

.button:hover {
  background-color: #0056b3;
  transform: scale(1.05);
}

/* Multiple transitions */
.element {
  transition: 
    opacity 0.3s ease,
    transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}`}</code></pre>
    </div>

    <h2 class="text-xl sm:text-2xl font-semibold text-custom-accent mt-6 mb-3">CSS Animations</h2>

    <div class="bg-custom-secondary border border-custom rounded-lg p-3 sm:p-4 my-4">
      <pre class="text-xs sm:text-sm overflow-x-auto whitespace-pre-wrap"><code>{`/* Define keyframes */
@keyframes slideIn {
  from {
    transform: translateX(-100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

/* Apply animation */
.element {
  animation: slideIn 0.5s ease-out;
  
  /* Individual properties */
  animation-name: slideIn;
  animation-duration: 0.5s;
  animation-timing-function: ease-out;
  animation-delay: 0s;
  animation-iteration-count: 1;
  animation-direction: normal;
  animation-fill-mode: both;
}

/* Multiple keyframes */
@keyframes bounce {
  0%, 20%, 50%, 80%, 100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-30px);
  }
  60% {
    transform: translateY(-15px);
  }
}`}</code></pre>
    </div>

    <div class="bg-custom-secondary border border-custom rounded-2xl p-4 sm:p-6 mt-6">
      <h2 class="text-xl sm:text-2xl font-semibold text-custom-accent mb-3">Animation Principles</h2>
      <ul class="list-disc list-inside space-y-2 pl-4">
        <li><strong class="text-custom-primary">Use transitions</strong> for simple state changes</li>
        <li><strong class="text-custom-primary">Use animations</strong> for complex, multi-step motions</li>
        <li><strong class="text-custom-primary">Consider performance</strong>: Prefer transform and opacity</li>
        <li><strong class="text-custom-primary">Respect user preferences</strong>: Use <code class="bg-custom-primary text-custom-secondary px-1 rounded text-sm">prefers-reduced-motion</code></li>
        <li><strong class="text-custom-primary">Timing functions</strong> create natural feeling motion</li>
      </ul>
    </div>
  </section>
</SidebarLayout>